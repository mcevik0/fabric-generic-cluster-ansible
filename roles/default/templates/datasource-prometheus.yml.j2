apiVersion: 1

datasources:
  - name: {{ grafana_prometheus_name }}
    type: prometheus
    access: proxy
    url: {{ grafana_prometheus_url }}
    isDefault: true
    editable: true
    jsonData:
      timeInterval: "15s"
